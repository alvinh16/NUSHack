#!/usr/bin/python3.9

def quotient(numerator, denominator):

    result = numerator / denominator

    return (result)

print ("This program does that which it does")
top = input("what is the numerator?")
bottom = input("what is the denominator?")

print ("the result is, ", quotient(float(top), float(bottom))) 
